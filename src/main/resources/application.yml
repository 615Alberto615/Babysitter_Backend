spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db_bs
    username: postgres
    password: AllSafe6
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true
